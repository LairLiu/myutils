console.log("this is main")

let body: HTMLElement;
function init() {
    body = document.body;
    console.log(body);
    var video = new lka.video("ios");
    // http://vjs.zencdn.net/v/oceans.mp4
    console.log(video);
};
init();  
